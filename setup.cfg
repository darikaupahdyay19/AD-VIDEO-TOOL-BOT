[flake8]
max-line-length = 100
extend-ignore = E203, W503, E704
exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    build,
    dist
per-file-ignores =
    bot/utils/queue.py: E501

[tool:pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
